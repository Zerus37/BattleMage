public enum DamageType
{
	pureMagic,
	death,
	fire,
	lightning,
	physycal
}

public enum SpellType
{
	projectile,
	raycast,
	selfCast,
	gravygun
}

public enum Directions
{
	forward,
	back,
	left,
	right
}